# Running the Project

Spring Boot (Maven) project. From the project root:

```bash
./mvnw spring-boot:run
```

## Other commands

- Build a jar: `./mvnw clean package` then `java -jar target/*.jar`
- Run tests: `./mvnw test`

Default URL: http://localhost:8080
